import { useEffect, useMemo, useState } from "react";
import { Link, useNavigate, useParams } from "react-router-dom";
import { ArrowLeft, Check, ChevronLeft, ChevronRight, Loader2, Palette, Pencil, Shapes, Trash2, X } from "lucide-react";
import type { Product, ProductVariant } from "@shared/api";
import { adminApi } from "@/lib/admin-api";

const money = (value?: number | null) => typeof value === "number" ? `${value.toFixed(2).replace(".", ",")} €` : "Prix non renseigné";
const inputClass = "h-11 w-full rounded-xl border border-[#d8e1d8] bg-[#fdfdfb] px-3 text-sm text-[#29493b] outline-none focus:border-[#4a8a63] focus:ring-4 focus:ring-[#e1eee2]";

export default function ProductDetail() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [product, setProduct] = useState<Product | null>(null);
  const [activeImage, setActiveImage] = useState(0);
  const [panel, setPanel] = useState<"info" | "colors" | "patterns" | null>(null);
  const [saving, setSaving] = useState(false);
  const [loading, setLoading] = useState(true);
  const [message, setMessage] = useState("");
  const [error, setError] = useState("");
  const [colorDraft, setColorDraft] = useState("");

  useEffect(() => { if (!id) return; adminApi.product(id).then(setProduct).catch((value) => setError(value instanceof Error ? value.message : "Produit introuvable")).finally(() => setLoading(false)); }, [id]);
  const images = useMemo(() => product ? (product.images?.map((image) => image.image_url).filter(Boolean) ?? []).concat(product.image_url && !(product.images?.some((image) => image.image_url === product.image_url)) ? [product.image_url] : []) : [], [product]);
  const colors = product?.colors ?? [];
  const save = async (payload: Parameters<typeof adminApi.updateProduct>[1], success: string) => { if (!product) return; setSaving(true); setError(""); setMessage(""); try { const updated = await adminApi.updateProduct(product.id, payload); setProduct((current) => current ? { ...current, ...updated } : current); setMessage(success); setPanel(null); } catch (value) { setError(value instanceof Error ? value.message : "Enregistrement impossible"); } finally { setSaving(false); } };
  const remove = async () => { if (!product || !window.confirm("Supprimer définitivement ce produit et ses relations ?")) return; setSaving(true); try { await adminApi.deleteProduct(product.id); navigate("/produits", { replace: true }); } catch (value) { setError(value instanceof Error ? value.message : "Suppression impossible"); setSaving(false); } };
  const updateVariant = (index: number, key: keyof ProductVariant, value: unknown) => { if (!product) return; const variants = [...(product.variants ?? [])]; variants[index] = { ...variants[index], [key]: value }; setProduct({ ...product, variants }); };

  if (loading) return <div className="flex justify-center py-24 text-[#6e8b78]"><Loader2 className="animate-spin" /></div>;
  if (!product) return <div className="space-y-4"><Link to="/produits" className="inline-flex items-center gap-2 text-sm font-bold text-[#3f7652]"><ArrowLeft size={16} /> Retour au catalogue</Link><div className="rounded-2xl border border-[#edcfc4] bg-[#fff5f1] px-5 py-4 text-sm text-[#a64f3b]">{error || "Produit introuvable"}</div></div>;

  return <div className="space-y-7">
    <Link to="/produits" className="inline-flex items-center gap-2 text-sm font-bold text-[#3f7652] hover:text-[#16543e]"><ArrowLeft size={16} /> Retour au catalogue</Link>
    {message && <div className="flex items-center gap-2 rounded-xl border border-[#cfe2d0] bg-[#edf7ee] px-4 py-3 text-sm text-[#3d7650]"><Check size={16} />{message}</div>}{error && <div className="rounded-xl border border-[#edcfc4] bg-[#fff5f1] px-4 py-3 text-sm text-[#a64f3b]">{error}</div>}
    <div className="grid gap-8 xl:grid-cols-[1.05fr_0.95fr]">
      <section><div className="relative aspect-[4/3] overflow-hidden rounded-3xl bg-[#eaf0e9]">{images.length ? <img src={images[activeImage] ?? images[0]} alt={product.name} className="h-full w-full object-cover" /> : <div className="flex h-full items-center justify-center text-[#789384]">Aucune image galerie</div>}{images.length > 1 && <><button onClick={() => setActiveImage((current) => (current - 1 + images.length) % images.length)} className="absolute left-4 top-1/2 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-[#fbfcf9]/90 text-[#365347]" aria-label="Image précédente"><ChevronLeft size={20} /></button><button onClick={() => setActiveImage((current) => (current + 1) % images.length)} className="absolute right-4 top-1/2 flex h-10 w-10 -translate-y-1/2 items-center justify-center rounded-full bg-[#fbfcf9]/90 text-[#365347]" aria-label="Image suivante"><ChevronRight size={20} /></button></>}</div>{images.length > 1 && <div className="mt-3 grid grid-cols-5 gap-3">{images.map((image, index) => <button key={image + index} onClick={() => setActiveImage(index)} className={`aspect-square overflow-hidden rounded-xl border-2 ${activeImage === index ? "border-[#16543e]" : "border-transparent"}`}><img src={image} alt={`${product.name} ${index + 1}`} className="h-full w-full object-cover" /></button>)}</div>}</section>
      <section className="flex flex-col rounded-3xl border border-[#dfe6dd] bg-[#fbfcf9] p-6 md:p-8"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#a17745]">Fiche produit</p><h1 className="mt-3 font-serif text-4xl leading-tight text-[#18352b]">{product.name}</h1><p className="mt-4 text-2xl font-semibold text-[#a17745]">{money(product.price ?? product.base_price)}</p><p className="mt-6 whitespace-pre-line text-sm leading-7 text-[#687b70]">{product.description || "Aucune description renseignée."}</p><div className="mt-8 grid gap-4 border-t border-[#e7ece5] pt-6 sm:grid-cols-2"><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#91a098]">Couleurs</p><p className="mt-2 text-sm font-semibold text-[#365347]">{colors.length ? colors.join(", ") : "Non renseignées"}</p></div><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-[#91a098]">Tailles</p><p className="mt-2 text-sm font-semibold text-[#365347]">{product.variants?.length || 0} variante(s)</p></div></div><div className="mt-auto grid gap-2 pt-8 sm:grid-cols-2"><button onClick={() => setPanel("colors")} className="inline-flex items-center justify-center gap-2 rounded-xl border border-[#cdddcf] px-3 py-3 text-xs font-bold text-[#3f7652] hover:bg-[#eef6ef]"><Palette size={15} /> Modifier couleurs disponibles</button><button onClick={() => setPanel("patterns")} className="inline-flex items-center justify-center gap-2 rounded-xl border border-[#cdddcf] px-3 py-3 text-xs font-bold text-[#3f7652] hover:bg-[#eef6ef]"><Shapes size={15} /> Modifier motifs disponibles</button><button onClick={() => setPanel("info")} className="inline-flex items-center justify-center gap-2 rounded-xl bg-[#16543e] px-3 py-3 text-xs font-bold text-white hover:bg-[#0f4532]"><Pencil size={15} /> Modifier informations</button><button onClick={remove} disabled={saving} className="inline-flex items-center justify-center gap-2 rounded-xl border border-[#edcfc4] px-3 py-3 text-xs font-bold text-[#a64f3b] hover:bg-[#fff2ed]"><Trash2 size={15} /> Supprimer produit</button></div></section>
    </div>
    <section className="rounded-2xl border border-[#dfe6dd] bg-[#fbfcf9] p-6"><p className="text-xs font-bold uppercase tracking-[0.17em] text-[#a17745]">Variantes disponibles</p><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{product.variants?.length ? product.variants.map((variant) => <div key={String(variant.id)} className="rounded-xl border border-[#e5ebe4] bg-[#fdfdfb] p-4"><p className="text-sm font-bold text-[#365347]">{variant.size_label || variant.size || "Taille non renseignée"}</p><p className="mt-1 text-xs text-[#7f9086]">{variant.dimensions || "Dimensions non renseignées"}</p><p className="mt-3 text-sm font-bold text-[#a17745]">{money(variant.price)}</p></div>) : <p className="text-sm text-[#91a098]">Aucune variante enregistrée.</p>}</div></section>
    <section className="rounded-2xl border border-[#dfe6dd] bg-[#fbfcf9] p-6"><p className="text-xs font-bold uppercase tracking-[0.17em] text-[#a17745]">Motifs disponibles</p><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{product.patterns?.length ? product.patterns.map((pattern) => <div key={pattern.id} className="overflow-hidden rounded-xl border border-[#e5ebe4] bg-[#fdfdfb]"><div className="aspect-square bg-[#edf2ec]">{pattern.thumbnail_url && <img src={pattern.thumbnail_url} alt={pattern.name} className="h-full w-full object-cover" />}</div><div className="p-3"><p className="text-sm font-bold text-[#365347]">{pattern.name}</p><p className="mt-1 text-xs text-[#8b9a90]">{pattern.primary_color || "Couleur non renseignée"}</p></div></div>) : <p className="text-sm text-[#91a098]">Aucun motif disponible.</p>}</div></section>
    {panel && <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#18352b]/35 p-0 sm:items-center sm:p-6"><div className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-t-3xl bg-[#fbfcf9] p-6 shadow-2xl sm:rounded-3xl md:p-8"><div className="mb-6 flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.17em] text-[#a17745]">Édition produit</p><h2 className="mt-1 font-serif text-2xl text-[#18352b]">{panel === "info" ? "Informations" : panel === "colors" ? "Couleurs disponibles" : "Motifs disponibles"}</h2></div><button onClick={() => setPanel(null)} className="rounded-xl p-2 text-[#8b9a90] hover:bg-[#eef3ed]" aria-label="Fermer"><X size={20} /></button></div>{panel === "info" && <div className="space-y-4"><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[0.12em] text-[#718279]">Nom</span><input className={inputClass} value={product.name} onChange={(event) => setProduct({ ...product, name: event.target.value })} /></label><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[0.12em] text-[#718279]">Description</span><textarea className={`${inputClass} h-28 py-3`} value={product.description ?? ""} onChange={(event) => setProduct({ ...product, description: event.target.value })} /></label><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[0.12em] text-[#718279]">Prix de base (€)</span><input type="number" step="0.01" className={inputClass} value={product.base_price ?? product.price ?? 0} onChange={(event) => setProduct({ ...product, base_price: Number(event.target.value) })} /></label><div className="space-y-2"><p className="text-xs font-bold uppercase tracking-[0.12em] text-[#718279]">Tailles</p>{(product.variants ?? []).map((variant, index) => <div key={String(variant.id ?? index)} className="grid gap-2 sm:grid-cols-3"><input className={inputClass} placeholder="Taille" value={variant.size_label ?? variant.size ?? ""} onChange={(event) => updateVariant(index, "size_label", event.target.value)} /><input className={inputClass} placeholder="Dimensions" value={variant.dimensions ?? ""} onChange={(event) => updateVariant(index, "dimensions", event.target.value)} /><input className={inputClass} type="number" placeholder="Prix" value={variant.price ?? 0} onChange={(event) => updateVariant(index, "price", Number(event.target.value))} /></div>)}</div><button disabled={saving} onClick={() => save({ name: product.name, description: product.description ?? "", price: product.base_price ?? product.price ?? 0, variants: product.variants ?? [] }, "Informations enregistrées dans Supabase.")} className="mt-4 w-full rounded-xl bg-[#16543e] px-4 py-3 text-sm font-bold text-white">{saving ? "Enregistrement…" : "Enregistrer les informations"}</button></div>}{panel === "colors" && <div className="space-y-4"><p className="text-sm text-[#718279]">Séparez les couleurs par des virgules.</p><input className={inputClass} value={colorDraft || colors.join(", ")} onChange={(event) => setColorDraft(event.target.value)} placeholder="Sauge, écru, terracotta" /><button disabled={saving} onClick={() => save({ colors: (colorDraft || colors.join(", ")).split(",").map((color) => color.trim()).filter(Boolean) }, "Couleurs enregistrées dans Supabase.")} className="w-full rounded-xl bg-[#16543e] px-4 py-3 text-sm font-bold text-white">Enregistrer les couleurs</button></div>}{panel === "patterns" && <div className="space-y-4"><p className="text-sm text-[#718279]">Les motifs sont gérés depuis les entrées `product_patterns` liées à ce produit.</p>{product.patterns?.length ? <div className="space-y-2">{product.patterns.map((pattern) => <div key={pattern.id} className="flex items-center gap-3 rounded-xl border border-[#e5ebe4] p-3"><span className="h-9 w-9 overflow-hidden rounded-lg bg-[#edf2ec]">{pattern.thumbnail_url && <img src={pattern.thumbnail_url} alt="" className="h-full w-full object-cover" />}</span><span className="text-sm font-bold text-[#365347]">{pattern.name}</span></div>)}</div> : <p className="text-sm text-[#91a098]">Aucun motif lié.</p>}<button onClick={() => setPanel(null)} className="w-full rounded-xl border border-[#cdddcf] px-4 py-3 text-sm font-bold text-[#3f7652]">Fermer</button></div>}</div></div>}
  </div>;
}
